{
  "rules": {
    ".read": "true",
    ".write": "true",
    "tags": {
      "en": {
        "$tagsId": {
          "name": {
            ".write": "!data.exists()"
          }
        }
      }
    }
  }
}